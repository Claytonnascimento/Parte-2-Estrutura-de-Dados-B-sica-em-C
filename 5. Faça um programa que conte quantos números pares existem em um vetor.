#include <stdio.h>

int main() {
    int v[5], contador = 0;

    for(int i = 0; i < 5; i++) {
        scanf("%d", &v[i]);
        if(v[i] % 2 == 0)
            contador++;
    }

    printf("Quantidade de pares: %d\n", contador);

    return 0;
}
